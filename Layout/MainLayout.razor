@inherits LayoutComponentBase

<MudThemeProvider />
<MudPopoverProvider />

<MudThemeProvider Theme="@CustomTheme"/>

<MudLayout>
    <MudAppBar Elevation="0"
        Style="background-color: #FAF9F6; color: #A68966;">
        <MudText Typo="Typo.h6" Style="font-weight: 800;">.Home</MudText>
        <MudSpacer />
        <MudIconButton Icon="@Icons.Custom.Brands.Instagram"
            Color="Color.Inherit" Link="你的IG連結" Target="_blank" />
        <MudIconButton Icon="@Icons.Custom.Brands.GitHub"
            Color="Color.Inherit" Link="你的GitHub連結" Target="_blank" />
    </MudAppBar>

    <MudMainContent Class="pb-16" Style="background-color: #FAF9F6;">
        @Body
    </MudMainContent>

    <MudAppBar Bottom="true" Fixed="true" Elevation="4"
        Style="background-color: rgba(255,255,255,0.8);
            backdrop-filter: blur(10px);">
        <MudSpacer />
        <MudIconButton Icon="@Icons.Material.Filled.Home"
            Color="Color.Primary" />
        <MudSpacer />
        <MudIconButton Icon="@Icons.Material.Filled.DirectionsBike"
            Color="Color.Primary" />
        <MudSpacer />
        <MudIconButton Icon="@Icons.Material.Filled.Coffee"
            Color="Color.Primary" />
        <MudSpacer />
        <MudIconButton Icon="@Icons.Material.Filled.Pets"
            Color="Color.Primary" />
        <MudSpacer />
    </MudAppBar>
</MudLayout>

@code {
    private MudTheme CustomTheme = new MudTheme()
    {
        PaletteLight = new PaletteLight()
        {
            Primary = "#E6D5B8",          // 杏色/奶茶色 (主要色)
            Secondary = "#F0E5D1",        // 淺米色 (次要色)
            Background = "#FFFDF9",       // 奶油白背景
            AppbarBackground = "#FFFDF9", // 與背景統一，營造無邊框感
            Surface = "#FFFFFF",          // 卡片表面顏色
            TextPrimary = "#5F4B32",      // 深咖色文字
            TextSecondary = "#8D7B68",    // 淺咖色文字
            ActionDefault = "#E6D5B8",    // 圖標與動作顏色
            DrawerBackground = "#FFFDF9",
            Divider = "#F0E5D1"
        },
        LayoutProperties = new LayoutProperties()
        {
            DefaultBorderRadius = "20px", // 關鍵：讓所有按鈕、卡片都變圓潤
            DrawerWidthLeft = "260px"
        },
        Typography = new Typography()
        {
            Default = new DefaultTypography()
            {
                FontFamily = new[] {
                    "Noto Sans TC", "微軟正黑體", "sans-serif"
                }
            }
        }
    };
}